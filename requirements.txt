# GenAR PADER — Python dependencies
# Core pipeline
pandas>=2.0
openpyxl>=3.1       # Excel ingestion
openai>=1.0         # Optional LLM client (review UI works without it)

# Review UI
flask>=3.0

# Testing
pytest>=8.0
